# GitHub Key Folder

This folder is reserved for your GitHub private key.

- Put the private SSH key here, for example `id_ed25519` or `id_rsa`.
- The folder is ignored by Git so your key is not committed.
- If you want, I can also help you move it to your SSH config and test the GitHub connection.
